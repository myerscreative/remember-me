# Supabase Configuration
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI API Key (required for Quick Voice Entry feature)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key

# OAuth Token Encryption Key (REQUIRED for security)
# Generate a secure 256-bit key with: openssl rand -base64 32
# This key is used to encrypt calendar OAuth tokens in the database
ENCRYPTION_KEY=your_encryption_key_here

# Google Calendar OAuth (optional - for calendar integration)
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Microsoft Calendar OAuth (optional - for calendar integration)
# Create OAuth app at: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
NEXT_PUBLIC_MICROSOFT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret

# Base URL for OAuth callbacks
# In development: http://localhost:3000
# In production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
